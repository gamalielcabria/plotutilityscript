% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_abundance_plot.R
\name{nested_abundance_plot}
\alias{nested_abundance_plot}
\title{Create a nested abundance plot}
\usage{
nested_abundance_plot(
  data,
  x_col,
  y_col,
  fill_col,
  split_col,
  nested_cols,
  y_label = "Relative abundance",
  fill_label = NULL,
  y_limits = c(0, 1),
  y_breaks = seq(0, 1, 0.2),
  season_gap = 0.02,
  layout_widths = c(0.01, 1, 0.18),
  bar_width = 0.95,
  palette = NULL,
  fill_scale = NULL,
  colour_scale = NULL,
  plot_theme = NULL,
  nest_line_colour = "grey50",
  nest_line_linetype = "dotted",
  nest_line_linewidth = 0.5,
  strip_fill = "white",
  strip_colour = "white",
  strip_text_colour = "black",
  strip_text_face = c("bold", rep("plain", length(nested_cols) - 1)),
  strip_text_size = c(10, rep(6, length(nested_cols) - 1)),
  border_colour = "black",
  border_linewidth = 1
)
}
\arguments{
\item{data}{Data frame in long format.}

\item{x_col}{Column mapped to the x-axis.}

\item{y_col}{Column mapped to the y-axis.}

\item{fill_col}{Column used for fill colours.}

\item{split_col}{Column used to split the plot into separate panels.}

\item{nested_cols}{Character vector of columns used as nested facets.}

\item{y_label}{Label for the y-axis.}

\item{fill_label}{Label for the legend. If \code{NULL}, the fill column name is used.}

\item{y_limits}{Numeric vector of length 2 giving y-axis limits.}

\item{y_breaks}{Numeric vector specifying y-axis breaks.}

\item{season_gap}{Relative spacing between split panels.}

\item{layout_widths}{Numeric vector of length 3 controlling widths of y-axis,
main panel, and legend.}

\item{bar_width}{Width of abundance bars.}

\item{palette}{Optional named vector of fill colours. Names should match the
values in \code{fill_col}. Ignored if \code{fill_scale} is supplied.}

\item{fill_scale}{Optional ggplot2 fill scale, such as
\code{ggplot2::scale_fill_brewer()} or \code{ggplot2::scale_fill_manual()}.}

\item{colour_scale}{Optional ggplot2 colour scale for future extensions or
plots that use colour aesthetics.}

\item{plot_theme}{Optional ggplot2 theme object applied after package
defaults. Useful for modifying text, axes, legends, margins, and other
theme elements.}

\item{nest_line_colour}{Colour of nested facet connector lines.}

\item{nest_line_linetype}{Linetype of nested facet connector lines.}

\item{nest_line_linewidth}{Linewidth of nested facet connector lines.}

\item{strip_fill}{Fill colour for nested strip backgrounds.}

\item{strip_colour}{Border colour for nested strip backgrounds.}

\item{strip_text_colour}{Text colour for nested strip labels.}

\item{strip_text_face}{Font face for nested strip labels.}

\item{strip_text_size}{Text size for nested strip labels.}

\item{border_colour}{Colour of the border around each split plot.}

\item{border_linewidth}{Linewidth of the border around each split plot.}
}
\value{
A patchwork object containing the combined abundance plot.
}
\description{
Create a faceted abundance plot using nested grouping variables. The function
automatically splits the plot by a higher-level grouping variable, draws one
nested plot per group, adds a shared y-axis and legend, and combines all
panels with patchwork.
}
\details{
The input data must be in long format with one row per observation. The split
column is rendered as separate plot blocks, while the nested columns are
rendered using \code{\link[ggh4x:facet_nested]{ggh4x::facet_nested()}}. All split panels share the same y-axis
limits.

Colours can be controlled in three ways:
\enumerate{
\item Leave \code{palette} and \code{fill_scale} as \code{NULL} to use the default ggplot2 fill colours.
\item Provide a named vector to \code{palette}.
\item Provide a full ggplot2 scale to \code{fill_scale}.
}

If both \code{palette} and \code{fill_scale} are supplied, \code{fill_scale} is used.
}
\examples{
\dontrun{
p <- nested_abundance_plot(
  data = plot_df,
  x_col = Duplicate,
  y_col = RelAbund,
  fill_col = Phylum,
  split_col = Season,
  nested_cols = c("Location", "Week")
)

my_palette <- c(
  Bacillota = "#f38400",
  Bacteroidota = "#be0032",
  Pseudomonadota = "#0067a5"
)

p2 <- nested_abundance_plot(
  data = plot_df,
  x_col = Duplicate,
  y_col = RelAbund,
  fill_col = Phylum,
  split_col = Season,
  nested_cols = c("Location", "Week"),
  palette = my_palette,
  plot_theme = ggplot2::theme(
    axis.text.x = ggplot2::element_text(angle = 45, hjust = 1)
  )
)

p3 <- nested_abundance_plot(
  data = plot_df,
  x_col = Duplicate,
  y_col = RelAbund,
  fill_col = Phylum,
  split_col = Season,
  nested_cols = c("Location", "Week"),
  fill_scale = ggplot2::scale_fill_brewer(palette = "Set3")
)
}

}
